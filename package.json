{
  "name": "ar-io-anchor",
  "version": "0.0.0",
  "description": "TypeScript anchoring SDK for the ar.io verification stack — anchor-at-write-time: signed event envelopes (ario.events/v1) to Arweave via Turbo, with Merkle-batched checkpoints and per-event inclusion proofs.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @ar.io/anchor",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/anchor"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
