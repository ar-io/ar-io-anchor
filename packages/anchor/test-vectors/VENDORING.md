# Vendored conformance vectors

`ario.events-v1/` is vendored byte-for-byte from `ar-io-proof`
`test-vectors/ario.events-v1/` at corpus tag **`test-vectors-v1.1`**
(ceremony commit `9a9c7dc`, 2026-06-11 — generated by `tools/gen-vectors/`,
never hand-edited; governance §4 CORP1). Re-pinning to a newer corpus tag is
this repo's explicit act: sync the files from the tagged commit and update
the tag recorded here.

Sync:

```bash
cp ../ar-io-proof/test-vectors/ario.events-v1/*.json packages/anchor/test-vectors/ario.events-v1/
```
