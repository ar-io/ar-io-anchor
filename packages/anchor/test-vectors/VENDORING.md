# Vendored conformance vectors

`ario.events-v1/` is vendored byte-for-byte from `ar-io-proof`
`test-vectors/ario.events-v1/` — currently the **`test-vectors-v1.1` candidate
set** staged in [ar-io-proof#3](https://github.com/ar-io/ar-io-proof/pull/3)
(generated by `tools/gen-vectors/`, never hand-edited — governance §4 CORP1).

When the `test-vectors-v1.1` tag is cut in `ar-io-proof`, re-pin: sync the
files from the tagged commit and record the tag here. Until then this copy
pins the candidate bytes at `ar-io-proof` commit `3d5014f`.

Sync:

```bash
cp ../ar-io-proof/test-vectors/ario.events-v1/*.json packages/anchor/test-vectors/ario.events-v1/
```
